#!/bin/sh
# Runs the suite checks before every push; enable with:
#   git config core.hooksPath .githooks
set -e
root="$(git rev-parse --show-toplevel)"
python3 "$root/tools/validate_suite.py"
python3 "$root/tools/reference_check.py"
python3 "$root/tools/run_tests.py"
