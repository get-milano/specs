{
  "name": "expr-double-modulo-non-finite",
  "description": "Double % follows IEEE 754 with non-finite operands: an infinite dividend or a zero divisor is nan, an infinite divisor leaves the dividend alone, and nothing is reported",
  "document": {
    "version": "1.0.0",
    "root": {
      "type": "Text",
      "id": "result",
      "properties": {
        "text": {
          "$expr": "str((1.0 / 0.0) % 2.0) + ' ' + str(2.0 % (1.0 / 0.0)) + ' ' + str(2.0 % 0.0) + ' ' + str((0.0 - 2.0) % (1.0 / 0.0))"
        }
      }
    }
  },
  "expect": {
    "view": {
      "type": "Text",
      "reference": "result",
      "properties": {
        "text": "nan 2.0 nan -2.0"
      }
    },
    "occurrences": []
  }
}
